'use strict';

import { View } from '../../lib/view';

export class AccountsView extends View {
  init() {
    // for anything that needs to happen at instantiation
  }

  someCustomFunc() {
    
  }
}
